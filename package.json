{
  "name": "agente-window",
  "version": "1.0.0",
  "private": true,
  "description": "AGENTE WINDOW — workbench modular inspirado no VS Code — arquitetura híbrida em platform/",
  "type": "module",
  "workspaces": [
    "legacy/AMBIENTE_COMPLETO_PARA_OPENCLAUDE/02_replica_final",
    "platform/services/pty-server"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "typecheck:contracts": "tsc -p tsconfig.contracts.json --noEmit",
    "typecheck:all": "npm run typecheck:contracts && npm run typecheck --workspace=@agente-window/pty-server && npm run typecheck --workspace=agents-window-replica",
    "test": "vitest run",
    "test:unit": "vitest run platform/apps/workbench/tests/unit",
    "dev:frontend": "npm run dev --workspace=agents-window-replica",
    "dev:pty": "npm run dev --workspace=@agente-window/pty-server"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
